<div class="row mt-5">
    <div class="col-md-6 m-auto">
        <div class="card card-body">
              <%# Partials %>
            <%- include('./partials/messages.ejs'); -%> 
                    <form action="/users/reset" method="POST">
                        <div class="form-group">
                            <label for="Email">Email address</label>
                            <input type="email" name="email" id="" required class="form-control"
                                value="<%= typeof email != 'undefined' ? email : '' %>">
                        </div>
                        <div class="form-group text-center">
                            <button type="submit" class="btn btn-danger btn-block mt-4">Submit</button>
                        </div> 
                        <p>No Account <a href="/dashboard" class="p-3 bg-info bg-opacity-10 border border-info border-start-0 rounded-end text-decoration-none "> Back</a></p>
                    </form>
        </div>
    </div>
</div>